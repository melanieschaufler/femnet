<!-- Head -->

<% content_for :meta_title, "#{DEFAULT_META["meta_product_name"]} | Edit Interests " %>

<!-- Body -->


<%= render partial: 'form', locals: { user_interest: @user_interest, interests: @interests} %>
